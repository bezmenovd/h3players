services:
  web:
    image: node:22
    container_name: h3players-web
    restart: unless-stopped
    ports:
      - 5173:5173
    networks:
      - internal
    depends_on:
      - mysql
    working_dir: /var/www
    volumes:
      - ./:/var/www
    command: bash -c "yarn && yarn run web:dev"

  api:
    image: node:22
    container_name: h3players-api
    restart: unless-stopped
    networks:
      - internal
    depends_on:
      - mysql
    working_dir: /var/www
    volumes:
      - ./:/var/www
    command: bash -c "yarn && yarn run api:start:dev"

  nginx:
    image: nginx:alpine
    container_name: h3players-nginx
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - ./docker/nginx/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
      - ./:/var/www:ro
    depends_on:
      - web
    networks:
      - internal

  mysql:
    image: mysql/mysql-server:8.0
    container_name: h3players-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: database
      MYSQL_USER: user
      MYSQL_PASSWORD: jk7S91xAC5bE5l3I
      MYSQL_ALLOW_EMPTY_PASSWORD: 1
    volumes:
      - mysql-volume:/var/lib/mysql
    networks:
      - internal

  redis:
    image: redis:alpine
    container_name: h3players-redis
    restart: unless-stopped
    volumes:
      - redis-volume:/data
    networks:
      - internal

  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    container_name: h3players-clickhouse
    restart: unless-stopped
    environment:
      - CLICKHOUSE_PASSWORD=xQm8LpsLOolVLryE
    volumes:
      - clickhouse-volume:/var/lib/clickhouse
      - ./docker/clickhouse/initdb:/docker-entrypoint-initdb.d:ro
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

networks:
  internal:
    driver: bridge

volumes:
  mysql-volume:
    driver: local
  redis-volume:
    driver: local
  clickhouse-volume:
    driver: local
